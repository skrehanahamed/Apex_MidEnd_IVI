name: Release

on:
  push:
    tags:
      - 'v*.*.*'
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      tag_name:
        description: 'Release tag name (e.g. v1.0.0)'
        required: false
        default: ''

jobs:
  build-linux:
    name: Build Linux (Ubuntu 22.04)
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Install Linux Dependencies
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          sudo rm -f /etc/apt/apt.conf.d/20auto-upgrades
          sudo apt-get -o Acquire::Retries=3 update -qq
          sudo apt-get -o Acquire::Retries=3 install -y --no-install-recommends \
            build-essential \
            cmake \
            ninja-build \
            libgl1-mesa-dev \
            libxkbcommon-dev \
            libxkbcommon-x11-dev \
            libegl1-mesa-dev \
            libfontconfig1-dev \
            libfreetype6-dev \
            libasound2-dev \
            libpulse-dev \
            zip

      - name: Setup Qt 6
        uses: jurplel/install-qt-action@v3
        with:
          version: '6.5.3'
          arch: gcc_64
          modules: 'qtmultimedia'
          cache: true
          cache-key-prefix: 'install-qt-action-6.5.3-linux'

      - name: Configure CMake
        run: |
          cmake -B build -G Ninja -DCMAKE_BUILD_TYPE=Release

      - name: Build Application
        run: |
          cmake --build build --config Release --parallel

      - name: Package Linux Binary
        run: |
          mkdir -p package/ApexIVI-Ubuntu-x86_64
          cp build/ApexIVI package/ApexIVI-Ubuntu-x86_64/
          chmod +x package/ApexIVI-Ubuntu-x86_64/ApexIVI
          echo '#!/bin/bash' > package/ApexIVI-Ubuntu-x86_64/run.sh
          echo 'DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"' >> package/ApexIVI-Ubuntu-x86_64/run.sh
          echo '"$DIR/ApexIVI" "$@"' >> package/ApexIVI-Ubuntu-x86_64/run.sh
          chmod +x package/ApexIVI-Ubuntu-x86_64/run.sh
          cd package
          zip -r ../ApexIVI-Ubuntu-x86_64.zip ApexIVI-Ubuntu-x86_64
          cd ..

      - name: Upload Linux Artifact
        uses: actions/upload-artifact@v4
        with:
          name: ApexIVI-Ubuntu-x86_64
          path: ApexIVI-Ubuntu-x86_64.zip
          retention-days: 1

  build-macos:
    name: Build macOS
    runs-on: macos-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Install Qt and Ninja via Homebrew
        run: |
          brew install qt qtmultimedia ninja

      - name: Configure CMake
        run: |
          cmake -B build -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_PREFIX_PATH="$(brew --prefix qt);$(brew --prefix qtmultimedia)"

      - name: Build Application
        run: |
          cmake --build build --config Release --parallel

      - name: Package macOS Binary
        run: |
          mkdir -p package/ApexIVI-macOS
          cp build/ApexIVI package/ApexIVI-macOS/
          chmod +x package/ApexIVI-macOS/ApexIVI
          echo '#!/bin/bash' > package/ApexIVI-macOS/run.sh
          echo 'DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"' >> package/ApexIVI-macOS/run.sh
          echo '"$DIR/ApexIVI" "$@"' >> package/ApexIVI-macOS/run.sh
          chmod +x package/ApexIVI-macOS/run.sh
          cd package
          zip -r ../ApexIVI-macOS.zip ApexIVI-macOS
          cd ..

      - name: Upload macOS Artifact
        uses: actions/upload-artifact@v4
        with:
          name: ApexIVI-macOS
          path: ApexIVI-macOS.zip
          retention-days: 1

  build-windows:
    name: Build Windows
    runs-on: windows-2022

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Qt 6
        uses: jurplel/install-qt-action@v3
        with:
          version: '6.5.3'
          arch: win64_msvc2019_64
          modules: 'qtmultimedia'
          cache: true
          cache-key-prefix: 'install-qt-action-6.5.3-win-msvc2019'

      - name: Setup MSVC Developer Environment
        uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: x64

      - name: Install Ninja
        run: choco install ninja -y

      - name: Configure CMake
        shell: cmd
        run: |
          cmake -B build -G Ninja ^
            -DCMAKE_BUILD_TYPE=Release ^
            -DCMAKE_C_COMPILER=cl ^
            -DCMAKE_CXX_COMPILER=cl

      - name: Build Application
        shell: cmd
        run: |
          cmake --build build --config Release --parallel

      - name: Deploy Qt Runtime and Package Windows Binary
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path package/ApexIVI-Windows-x64
          Copy-Item -Path build/ApexIVI.exe -Destination package/ApexIVI-Windows-x64/
          if (Get-Command windeployqt -ErrorAction SilentlyContinue) {
              windeployqt --qmldir qml package/ApexIVI-Windows-x64/ApexIVI.exe
          } elseif ($env:QT_ROOT -and (Test-Path "$env:QT_ROOT/bin/windeployqt.exe")) {
              & "$env:QT_ROOT/bin/windeployqt.exe" --qmldir qml package/ApexIVI-Windows-x64/ApexIVI.exe
          }
          Compress-Archive -Path package/ApexIVI-Windows-x64 -DestinationPath ApexIVI-Windows-x64.zip

      - name: Upload Windows Artifact
        uses: actions/upload-artifact@v4
        with:
          name: ApexIVI-Windows-x64
          path: ApexIVI-Windows-x64.zip
          retention-days: 1

  release:
    name: Publish GitHub Release
    needs: [build-linux, build-macos, build-windows]
    runs-on: ubuntu-22.04
    permissions:
      contents: write

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Determine Release Version
        id: version
        run: |
          if [ -n "${{ github.event.inputs.tag_name }}" ]; then
            TAG="${{ github.event.inputs.tag_name }}"
          elif [ -n "${{ github.event.release.tag_name }}" ]; then
            TAG="${{ github.event.release.tag_name }}"
          elif [[ "${GITHUB_REF}" == refs/tags/* ]]; then
            TAG="${GITHUB_REF#refs/tags/}"
          else
            TAG=$(git describe --tags --abbrev=0 2>/dev/null || echo "v1.0.0")
          fi
          echo "VERSION=${TAG}" >> $GITHUB_OUTPUT

      - name: Generate Release Notes and Version Updates
        run: |
          python3 scripts/generate_release_notes.py "${{ steps.version.outputs.VERSION }}"

      - name: Download Build Artifacts
        uses: actions/download-artifact@v4
        with:
          path: release_assets
          merge-multiple: true

      - name: List Release Assets
        run: |
          ls -lh release_assets

      - name: Create or Update GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.version.outputs.VERSION }}
          name: "Apex MidEnd IVI - Digital Head Unit ${{ steps.version.outputs.VERSION }}"
          body_path: release_body.md
          files: release_assets/*.zip
          draft: false
          prerelease: false

      - name: Force Update Release Metadata
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release edit "${{ steps.version.outputs.VERSION }}" --title "Apex HORIZON MidEnd IVI - Digital Head Unit ${{ steps.version.outputs.VERSION }}" --notes-file release_body.md
